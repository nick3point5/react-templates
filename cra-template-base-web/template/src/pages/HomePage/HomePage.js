import React from 'react';
import './HomePage.css';

function HomePage () {
  const variant = 'primary'
  return (
    <div className={`HomePage ${variant}`}>
    </div>
  )
}

export default HomePage